spring:
  application:
    name: authService
  config:
    import: optional:configserver:http://localhost:8888
    # utilisation de MailDev pour tester l'envoi des emails, il faut configurer Docker et lancer le serveur de MailDev
    # sinon on peut utiliser le serveur SMTP de Gmail en ligne
  mail:
    host: smtp.gmail.com
    port: 587
    username: dhya.blel22@gmail.com
    password: rwvz gxvx vmun zyyl
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


application:
  security:
    jwt:
      secret-key: ${JWT_SECRET_KEY}
      expiration: 86400000 # a day
      refresh-token:
        expiration: 604800000 # 7 days
  mailing:
    frontend:
      activation-url: http://localhost:4200/auth/activate-account
      resetPassword-url: http://localhost:4200/auth/reset-password
  file:
    uploads:
      photos-output-path: ./uploads


